generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model User {
  id           Int      @id
  name         String
  email        String
  profileImage String
  createdAt    DateTime @default(now())
}
